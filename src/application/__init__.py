# Application Layer
#
# Use cases and orchestration. Depends on domain/ and ports/ only.
# Does NOT depend on infrastructure or external frameworks.
#
# Contains:
#   commands/ — Write operations (Command handlers)
#   queries/  — Read operations (Query handlers)
#   ports/    — Interfaces (Protocols) for infrastructure adapters
