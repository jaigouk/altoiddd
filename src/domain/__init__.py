# Domain Layer
#
# Core business logic. This layer has ZERO external dependencies.
# No frameworks, no database, no HTTP — only pure Python.
#
# Contains:
#   models/   — Entities, Value Objects, Aggregates
#   services/ — Domain Services (stateless business operations)
#   events/   — Domain Events
